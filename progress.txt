# Progress Log — Interview Trainer Bot

## Формат записей
Каждая итерация добавляет запись в формате:
```
## Итерация N — [Дата]
**Фича:** [Название]
**Статус:** [Завершено / В процессе / Заблокировано]
**Что сделано:**
- ...
**Следующие шаги:**
- ...
**Заметки для следующей итерации:**
- ...
```

---

## Итерация 1 — 2026-03-14
**Фича:** TASK-001 — Инициализация структуры монорепозитория и Docker Compose окружения
**Статус:** Завершено
**Что сделано:**
- Создана структура папок: backend/, frontend/, nginx/
- docker-compose.yml с 6 сервисами: db (pgvector/pg16), redis (7-alpine), backend, celery-worker, frontend, nginx
- Все сервисы имеют healthcheck и корректные depends_on
- .env.example со всеми переменными окружения
- backend/: Dockerfile (uv), pyproject.toml, minimal FastAPI app с /health, Celery placeholder, тест
- frontend/: Dockerfile, Vite+Vue3+Pinia+Router+ElementPlus, 4 views (Projects/Selection/History/Dictionaries)
- nginx/nginx.conf: /api/ → backend:8000, / → frontend:5173 с proxy_read_timeout 300s
- ruff OK, pytest OK (1 passed)
**Следующие шаги:**
- TASK-002: FastAPI config, Pydantic Settings, AsyncEngine + asyncpg, GET /health с проверкой DB, CORS
- TASK-003: SQLAlchemy ORM models, Alembic initial migration с pgvector
**Заметки для следующей итерации:**
- uv sync --extra dev нужен для ruff/pytest (dev optional deps)
- backend работает от python:3.11-slim, uv устанавливается через pip
- TASK-002 зависит только от TASK-001 (теперь done)
- Для celery-worker SYNC_DATABASE_URL использует psycopg2 (не asyncpg)

---

## Итерация 2 — 2026-03-15
**Фича:** TASK-002 — Настройка FastAPI-приложения: конфиг, БД, базовые зависимости, health-endpoint
**Статус:** Завершено
**Что сделано:**
- backend/app/config.py: Pydantic Settings (DATABASE_URL, SYNC_DATABASE_URL, REDIS_URL, SECRET_KEY, ALLOWED_ORIGINS)
- Авто-вывод sync_database_url из asyncpg URL (замена +asyncpg → +psycopg2)
- backend/app/database.py: AsyncEngine (create_async_engine) + async_sessionmaker + Base (DeclarativeBase) + get_db dependency
- backend/app/exceptions.py: NotFoundError (404), ConflictError (409), ValidationError (422)
- backend/app/main.py: /health проверяет подключение к PostgreSQL (SELECT 1), CORS с allowed_origins из settings
- tests/test_health.py: обновлён — мокает get_db через dependency_overrides, проверяет {status: ok, db: ok}
- ruff OK, pytest OK (1 passed)
**Следующие шаги:**
- TASK-003: SQLAlchemy ORM-модели + первая Alembic-миграция (все таблицы, pgvector)
- TASK-004: Celery + Redis (зависит от TASK-002, теперь done)
**Заметки для следующей итерации:**
- TASK-003 и TASK-004 оба зависят только от TASK-002 (теперь done) → можно брать любой из них
- Base (DeclarativeBase) находится в database.py — модели должны импортировать его оттуда
- Для Alembic env.py нужен sync engine (psycopg2), не asyncpg

---

## Итерация 3 — 2026-03-15
**Фича:** TASK-003 — ORM-модели SQLAlchemy и первая Alembic-миграция (все таблицы)
**Статус:** Завершено
**Что сделано:**
- Добавлен pgvector (0.4.2) + numpy в зависимости (pyproject.toml + uv.lock)
- backend/app/models.py: все 8 моделей с Mapped[] типами — Direction, PriorityDirection, TRLLevel, Group, Project, GroupingRun, SimilarityScore, RejectedPair
- Python Enum-классы: ProjectSource, GroupSource, GroupContext, RunStatus
- Project.embedding: Vector(312) через pgvector.sqlalchemy.Vector
- GIN-индекс для full-text search по projects (to_tsvector russian) через sa.text() в __table_args__
- UNIQUE constraint на (project_a_id, project_b_id) в SimilarityScore
- FK constraints с ondelete для всех внешних ключей
- Alembic инициализирован: alembic.ini + migrations/env.py (sync psycopg2 engine, settings.sync_database_url)
- migrations/versions/001_initial.py: CREATE EXTENSION IF NOT EXISTS vector, все таблицы, ENUM-типы через postgresql.ENUM, GIN-индекс через op.execute, downgrade
- ruff OK, pytest OK (1 passed)
**Следующие шаги:**
- TASK-004: Celery + Redis (зависит от TASK-002, теперь done)
- TASK-007: Справочники API (зависит от TASK-003, теперь done)
**Заметки для следующей итерации:**
- Alembic env.py использует sync engine (psycopg2), не asyncpg
- alembic upgrade head нужно запускать внутри docker compose exec backend
- Миграция создаёт ENUM типы вручную через postgresql.ENUM(...).create() перед созданием таблиц
- Vector(312) задаётся через ALTER TABLE после создания таблицы (placeholder Text → drop + add column)
- Base и модели: app.database.Base импортируется в app.models; env.py делает import app.models для регистрации

---

## Итерация 4 — 2026-03-15
**Фича:** TASK-004 — Настройка Celery + Redis: celery_app, конфигурация, тестовая задача
**Статус:** Завершено
**Что сделано:**
- celery_app.py: переключён с os.getenv на settings.REDIS_URL (консистентность с конфигом)
- Добавлен worker_init сигнал (load_models) — заготовка для загрузки ML-модели в TASK-012
- Глобальная переменная _embedding_model + get_embedding_model() для доступа к модели из задач
- docker-compose.yml: добавлен сервис flower (порт 5555, profile=monitoring — запускается только при --profile monitoring)
- tests/test_celery.py: 4 теста — ping возвращает 'pong', задача зарегистрирована, embedding model = None, broker/backend = redis
- ruff OK, pytest 5 passed (4 celery + 1 health)
**Следующие шаги:**
- TASK-007: Справочники API (depends on TASK-003, now done)
- TASK-005: Nginx конфигурация (depends on TASK-001, TASK-002, both done)
**Заметки для следующей итерации:**
- worker_init сигнал: `from celery.signals import worker_init` + `@worker_init.connect`
- _embedding_model — глобальная переменная в celery_app.py, TASK-012 должен её заполнить через load_models
- Flower запускается с профилем: `docker compose --profile monitoring up -d flower`
- celery-worker healthcheck отсутствует (service_started в зависимостях flower), это нормально

---

## Итерация 5 — 2026-03-15
**Фича:** TASK-007 — Справочники: API CRUD для directions, priority_directions, trl_levels
**Статус:** Завершено
**Что сделано:**
- backend/app/schemas/dictionary.py: DictionaryItemRead, DictionaryItemCreate, DictionaryItemUpdate (level: int | None для trl_levels)
- backend/app/repositories/dictionary.py: DictionaryRepo (Generic[T]) с get_all, get_by_id, create, update, deactivate
- backend/app/services/dictionary.py: DictionaryService — деактивация через is_active=False, не удаление; hasattr check для level при create/update
- backend/app/api/dictionaries.py: роутер /api/dictionaries/{type} — GET, POST, PATCH/{id}, DELETE/{id} (204)
- backend/app/main.py: include_router(dictionaries_router)
- migrations/versions/002_seed_dictionaries.py: 6 directions, 6 priority_directions, 7 trl_levels
- tests/test_dictionaries.py: 10 тестов (get_all, active_only, trl levels, create, update, delete, 404 cases, unknown type)
- ruff OK, pytest 15 passed
**Следующие шаги:**
- TASK-008: Projects API (depends on TASK-003, TASK-007 — оба done)
- TASK-005: Nginx конфигурация (depends on TASK-001, TASK-002 — оба done)
**Заметки для следующей итерации:**
- DictionaryRepo Generic[T] с TypeVar(T, Direction, PriorityDirection, TRLLevel) — лежит в app/repositories/dictionary.py
- hasattr(self.model, "level") используется в сервисе, чтобы не передавать level в Direction/PriorityDirection
- Seed migration ID="002", down_revision="001" — применяется после 001_initial
- DictionaryItemRead с from_attributes=True: level=None по умолчанию (Direction/PriorityDirection не имеют этого атрибута — AttributeError → default)

---

## Итерация 0 — Инициализация
**Фича:** Настройка проекта
**Статус:** Ожидает начала
**Что сделано:**
- Создан PRD документ
- Определён технический стек (Python 3.13, uv, aiogram, Supabase)
**Следующие шаги:**
- Инициализировать проект с uv
- Настроить структуру папок
- Добавить базовые зависимости
**Заметки для следующей итерации:**
- Начать с Phase 1: MVP Core
- Приоритет: базовая структура проекта и конфигурация

---
