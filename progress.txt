# Progress Log — Interview Trainer Bot

## Формат записей
Каждая итерация добавляет запись в формате:
```
## Итерация N — [Дата]
**Фича:** [Название]
**Статус:** [Завершено / В процессе / Заблокировано]
**Что сделано:**
- ...
**Следующие шаги:**
- ...
**Заметки для следующей итерации:**
- ...
```

---

## Итерация 1 — 2026-03-14
**Фича:** TASK-001 — Инициализация структуры монорепозитория и Docker Compose окружения
**Статус:** Завершено
**Что сделано:**
- Создана структура папок: backend/, frontend/, nginx/
- docker-compose.yml с 6 сервисами: db (pgvector/pg16), redis (7-alpine), backend, celery-worker, frontend, nginx
- Все сервисы имеют healthcheck и корректные depends_on
- .env.example со всеми переменными окружения
- backend/: Dockerfile (uv), pyproject.toml, minimal FastAPI app с /health, Celery placeholder, тест
- frontend/: Dockerfile, Vite+Vue3+Pinia+Router+ElementPlus, 4 views (Projects/Selection/History/Dictionaries)
- nginx/nginx.conf: /api/ → backend:8000, / → frontend:5173 с proxy_read_timeout 300s
- ruff OK, pytest OK (1 passed)
**Следующие шаги:**
- TASK-002: FastAPI config, Pydantic Settings, AsyncEngine + asyncpg, GET /health с проверкой DB, CORS
- TASK-003: SQLAlchemy ORM models, Alembic initial migration с pgvector
**Заметки для следующей итерации:**
- uv sync --extra dev нужен для ruff/pytest (dev optional deps)
- backend работает от python:3.11-slim, uv устанавливается через pip
- TASK-002 зависит только от TASK-001 (теперь done)
- Для celery-worker SYNC_DATABASE_URL использует psycopg2 (не asyncpg)

---

## Итерация 2 — 2026-03-15
**Фича:** TASK-002 — Настройка FastAPI-приложения: конфиг, БД, базовые зависимости, health-endpoint
**Статус:** Завершено
**Что сделано:**
- backend/app/config.py: Pydantic Settings (DATABASE_URL, SYNC_DATABASE_URL, REDIS_URL, SECRET_KEY, ALLOWED_ORIGINS)
- Авто-вывод sync_database_url из asyncpg URL (замена +asyncpg → +psycopg2)
- backend/app/database.py: AsyncEngine (create_async_engine) + async_sessionmaker + Base (DeclarativeBase) + get_db dependency
- backend/app/exceptions.py: NotFoundError (404), ConflictError (409), ValidationError (422)
- backend/app/main.py: /health проверяет подключение к PostgreSQL (SELECT 1), CORS с allowed_origins из settings
- tests/test_health.py: обновлён — мокает get_db через dependency_overrides, проверяет {status: ok, db: ok}
- ruff OK, pytest OK (1 passed)
**Следующие шаги:**
- TASK-003: SQLAlchemy ORM-модели + первая Alembic-миграция (все таблицы, pgvector)
- TASK-004: Celery + Redis (зависит от TASK-002, теперь done)
**Заметки для следующей итерации:**
- TASK-003 и TASK-004 оба зависят только от TASK-002 (теперь done) → можно брать любой из них
- Base (DeclarativeBase) находится в database.py — модели должны импортировать его оттуда
- Для Alembic env.py нужен sync engine (psycopg2), не asyncpg

---

## Итерация 3 — 2026-03-15
**Фича:** TASK-003 — ORM-модели SQLAlchemy и первая Alembic-миграция (все таблицы)
**Статус:** Завершено
**Что сделано:**
- Добавлен pgvector (0.4.2) + numpy в зависимости (pyproject.toml + uv.lock)
- backend/app/models.py: все 8 моделей с Mapped[] типами — Direction, PriorityDirection, TRLLevel, Group, Project, GroupingRun, SimilarityScore, RejectedPair
- Python Enum-классы: ProjectSource, GroupSource, GroupContext, RunStatus
- Project.embedding: Vector(312) через pgvector.sqlalchemy.Vector
- GIN-индекс для full-text search по projects (to_tsvector russian) через sa.text() в __table_args__
- UNIQUE constraint на (project_a_id, project_b_id) в SimilarityScore
- FK constraints с ondelete для всех внешних ключей
- Alembic инициализирован: alembic.ini + migrations/env.py (sync psycopg2 engine, settings.sync_database_url)
- migrations/versions/001_initial.py: CREATE EXTENSION IF NOT EXISTS vector, все таблицы, ENUM-типы через postgresql.ENUM, GIN-индекс через op.execute, downgrade
- ruff OK, pytest OK (1 passed)
**Следующие шаги:**
- TASK-004: Celery + Redis (зависит от TASK-002, теперь done)
- TASK-007: Справочники API (зависит от TASK-003, теперь done)
**Заметки для следующей итерации:**
- Alembic env.py использует sync engine (psycopg2), не asyncpg
- alembic upgrade head нужно запускать внутри docker compose exec backend
- Миграция создаёт ENUM типы вручную через postgresql.ENUM(...).create() перед созданием таблиц
- Vector(312) задаётся через ALTER TABLE после создания таблицы (placeholder Text → drop + add column)
- Base и модели: app.database.Base импортируется в app.models; env.py делает import app.models для регистрации

---

## Итерация 4 — 2026-03-15
**Фича:** TASK-004 — Настройка Celery + Redis: celery_app, конфигурация, тестовая задача
**Статус:** Завершено
**Что сделано:**
- celery_app.py: переключён с os.getenv на settings.REDIS_URL (консистентность с конфигом)
- Добавлен worker_init сигнал (load_models) — заготовка для загрузки ML-модели в TASK-012
- Глобальная переменная _embedding_model + get_embedding_model() для доступа к модели из задач
- docker-compose.yml: добавлен сервис flower (порт 5555, profile=monitoring — запускается только при --profile monitoring)
- tests/test_celery.py: 4 теста — ping возвращает 'pong', задача зарегистрирована, embedding model = None, broker/backend = redis
- ruff OK, pytest 5 passed (4 celery + 1 health)
**Следующие шаги:**
- TASK-007: Справочники API (depends on TASK-003, now done)
- TASK-005: Nginx конфигурация (depends on TASK-001, TASK-002, both done)
**Заметки для следующей итерации:**
- worker_init сигнал: `from celery.signals import worker_init` + `@worker_init.connect`
- _embedding_model — глобальная переменная в celery_app.py, TASK-012 должен её заполнить через load_models
- Flower запускается с профилем: `docker compose --profile monitoring up -d flower`
- celery-worker healthcheck отсутствует (service_started в зависимостях flower), это нормально

---

## Итерация 5 — 2026-03-15
**Фича:** TASK-007 — Справочники: API CRUD для directions, priority_directions, trl_levels
**Статус:** Завершено
**Что сделано:**
- backend/app/schemas/dictionary.py: DictionaryItemRead, DictionaryItemCreate, DictionaryItemUpdate (level: int | None для trl_levels)
- backend/app/repositories/dictionary.py: DictionaryRepo (Generic[T]) с get_all, get_by_id, create, update, deactivate
- backend/app/services/dictionary.py: DictionaryService — деактивация через is_active=False, не удаление; hasattr check для level при create/update
- backend/app/api/dictionaries.py: роутер /api/dictionaries/{type} — GET, POST, PATCH/{id}, DELETE/{id} (204)
- backend/app/main.py: include_router(dictionaries_router)
- migrations/versions/002_seed_dictionaries.py: 6 directions, 6 priority_directions, 7 trl_levels
- tests/test_dictionaries.py: 10 тестов (get_all, active_only, trl levels, create, update, delete, 404 cases, unknown type)
- ruff OK, pytest 15 passed
**Следующие шаги:**
- TASK-008: Projects API (depends on TASK-003, TASK-007 — оба done)
- TASK-005: Nginx конфигурация (depends on TASK-001, TASK-002 — оба done)
**Заметки для следующей итерации:**
- DictionaryRepo Generic[T] с TypeVar(T, Direction, PriorityDirection, TRLLevel) — лежит в app/repositories/dictionary.py
- hasattr(self.model, "level") используется в сервисе, чтобы не передавать level в Direction/PriorityDirection
- Seed migration ID="002", down_revision="001" — применяется после 001_initial
- DictionaryItemRead с from_attributes=True: level=None по умолчанию (Direction/PriorityDirection не имеют этого атрибута — AttributeError → default)

---

## Итерация 6 — 2026-03-15
**Фича:** TASK-008 — Projects API: CRUD-эндпоинты и репозиторий
**Статус:** Завершено
**Что сделано:**
- backend/app/schemas/project.py: ProjectListItem, ProjectRead, ProjectCreate, ProjectUpdate, ProjectListResponse, StatsCounters, GroupInfo
- backend/app/repositories/project.py: ProjectRepo с get_list (фильтры + пагинация), get_by_id, create, update, delete, set_selected, count_stats; ProjectFilters dataclass
- backend/app/services/project.py: ProjectService — маппинг ORM→схемы, NotFoundError при отсутствии
- backend/app/api/projects.py: GET /api/projects (limit/offset/фильтры/поиск), GET /api/projects/{id}, POST /api/projects, PATCH /api/projects/{id}, DELETE /api/projects/{id}, POST /api/projects/{id}/select, DELETE /api/projects/{id}/select, GET /api/stats/counters
- Полнотекстовый поиск через plainto_tsquery('russian', :search) с bindparams
- app/main.py: include_router(projects_router)
- tests/test_projects.py: 11 тестов — список, фильтры, поиск, get, not_found, create, select/deselect, delete, stats, group_info
- ruff OK, pytest 26 passed
**Следующие шаги:**
- TASK-009: Импорт проектов из Excel (depends on TASK-008, now done)
- TASK-011: Groups API (depends on TASK-008, now done)
- TASK-005: Nginx конфигурация (depends on TASK-001, TASK-002, both done)
**Заметки для следующей итерации:**
- ProjectRepo._apply_filters: group_source использует JOIN с Group, has_group — просто WHERE
- Фильтр group_source и has_group взаимоисключающие (group_source имеет приоритет)
- selectinload используется для всех FK-отношений Project (direction, priority_direction, trl_level, group)
- count_stats возвращает total/new(is_auto_checked=False)/auto_checked(is_auto_checked=True)

---

## Итерация 7 — 2026-03-15
**Фича:** TASK-009 — Импорт проектов из Excel (.xlsx) с валидацией
**Статус:** Завершено
**Что сделано:**
- backend/app/schemas/excel_import.py: ImportRowError, ImportPreviewResponse
- backend/app/services/excel_import.py: ExcelImportService — parse_and_validate (openpyxl), do_import (session.add_all); build_template_xlsx()
- Валидация: обязательное поле title, lookup справочников (directions, priority_directions, trl_levels) по имени (case-insensitive), дубликаты в батче и в БД — warnings, не ошибки
- backend/app/api/projects.py: GET /api/projects/template (xlsx download), POST /api/projects/import (preview + confirm=true)
- Маршруты зарегистрированы ДО /{project_id} (избегаем конфликт с UUID path param)
- tests/test_import.py: 6 тестов (template, preview valid, preview errors, confirm, duplicate warning, empty confirm)
- ruff OK, pytest 32 passed
**Следующие шаги:**
- TASK-012: ML интеграция rubert-tiny2 (depends on TASK-004, TASK-008 — оба done); после реализации добавить вызов bulk_generate_embeddings в do_import
- TASK-011: Groups API (depends on TASK-008 — done)
- TASK-010: Экспорт в Excel (depends on TASK-008 — done)
**Заметки для следующей итерации:**
- ExcelImportService._load_dicts грузит активные словари; при импорте справочниковые значения ищутся по name.lower()
- do_import использует session.add_all + один commit (для скорости 1500 строк)
- ProjectSource.auto используется для импортированных проектов
- build_template_xlsx() — без зависимости от DB, чистая функция, опасен только для тестирования шаблона

---

## Итерация 8 — 2026-03-15
**Фича:** TASK-012 — ML: интеграция rubert-tiny2 через sentence-transformers в Celery-воркере
**Статус:** Завершено
**Что сделано:**
- Добавлены зависимости: sentence-transformers>=3.0.0, scikit-learn>=1.5.0, pymorphy3>=2.0.0, numpy>=1.26.0 в pyproject.toml
- backend/app/utils/text.py: TextProcessingUtils.prepare_text() — конкатенация title+problem+goal+expected_result, нижний регистр, удаление пунктуации, схлопывание пробелов
- backend/app/services/embedding.py: EmbeddingService.encode(texts) → np.ndarray (312-dim, normalize_embeddings=True); load_sentence_transformer() — загрузка cointegrated/rubert-tiny2
- backend/app/tasks/embeddings.py: Celery-задача bulk_generate_embeddings(project_ids) — берёт sync DB сессию (psycopg2), выбирает проекты без эмбеддингов, кодирует, сохраняет в БД
- backend/app/tasks/celery_app.py: worker_init теперь реально загружает rubert-tiny2 через load_sentence_transformer(); задача embeddings добавлена в include
- backend/app/services/excel_import.py: do_import() теперь запускает bulk_generate_embeddings.delay() после commit
- tests/test_embedding.py: 9 тестов — TextProcessingUtils (5 кейсов), EmbeddingService с mock (3 кейса), регистрация задачи и skipped при отсутствии модели
- ruff OK, pytest 42 passed
**Следующие шаги:**
- TASK-011: Groups API (depends on TASK-008 — done); нужна для TASK-013
- TASK-010: Экспорт в Excel (depends on TASK-008 — done)
- TASK-013: Авто-группировка (depends on TASK-011, TASK-012 — оба будут done)
**Заметки для следующей итерации:**
- bulk_generate_embeddings использует _get_sync_session() — создаёт новый sync engine на каждый вызов; для TASK-013 можно переиспользовать
- worker_init загружает модель при старте; в тестах _embedding_model=None (сигнал не срабатывает) — тест проверяет ветку skipped
- TextProcessingUtils._CLEAN_RE убирает всё кроме \w и \s (unicode-safe)
- EmbeddingService принимает модель в конструктор — удобно для мокирования в тестах

---

## Итерация 9 — 2026-03-15
**Фича:** TASK-011 — Groups API: CRUD для ручных и авто-групп
**Статус:** Завершено
**Что сделано:**
- backend/app/schemas/group.py: GroupRead, GroupListItem, GroupCreate (validator min 2 projects), GroupUpdate, GroupListResponse, AddProjectToGroup, ConflictingProject, GroupProjectItem
- backend/app/repositories/group.py: GroupRepo с get_all (фильтры по source/context/is_confirmed), get_by_id, get_projects_by_ids, create (flush + bulk update project group_id), update, delete (nullify project group_ids), confirm, add_project, remove_project
- backend/app/services/group.py: GroupService — conflict check (409 с деталями конфликтующих проектов), бизнес-логика для всех операций
- backend/app/api/groups.py: GET /api/groups, POST /api/groups, GET /api/groups/{id}, PATCH /api/groups/{id}, DELETE /api/groups/{id} (204), POST /api/groups/{id}/confirm, POST /api/groups/{id}/projects, DELETE /api/groups/{id}/projects/{project_id}
- backend/app/main.py: include_router(groups_router)
- tests/test_groups.py: 12 тестов — list, filters, create, min 2 projects (422), conflict (409), get, update, delete, confirm, add/remove project, not found
- ruff OK, pytest 54 passed
**Следующие шаги:**
- TASK-013: Алгоритм авто-группировки (depends on TASK-011, TASK-012 — оба done)
- TASK-010: Экспорт в Excel (depends on TASK-008 — done)
**Заметки для следующей итерации:**
- GroupRepo.create использует session.flush() для получения group.id до commit, затем bulk update Project.group_id
- GroupRepo.delete использует bulk update Project.group_id=null перед удалением группы
- ConflictError detail — dict с message + conflicting list (для совместимости с JSON-ответом FastAPI)
- GroupCreate.project_ids валидируется @field_validator (min 2 projects → 422 от Pydantic)
- TASK-013 теперь разблокирован (все его зависимости done)

---

## Итерация 10 — 2026-03-15
**Фича:** TASK-013 — Алгоритм авто-группировки: cosine similarity + agglomerative clustering
**Статус:** Завершено
**Что сделано:**
- backend/app/services/clustering.py: ClusteringService с тремя статическими методами:
  - cosine_similarity_matrix(embeddings) → матрица сходства через sklearn.metrics.pairwise.cosine_similarity
  - apply_rejected_pairs(matrix, project_ids, rejected_set) → обнуляет сходство для отклонённых пар (deep copy)
  - cluster(matrix, threshold) → AgglomerativeClustering(metric='precomputed', linkage='average', distance_threshold=1-threshold), возвращает группы (≥2 проектов)
- run_auto_grouping(session, threshold, context, grouping_run_id=None) — полный пайплайн:
  - Загружает проекты с эмбеддингами (для selection — только is_selected=True)
  - Загружает rejected_pairs для этих проектов из БД
  - Строит матрицу сходства и обнуляет rejected пары
  - Запускает кластеризацию
  - Удаляет старые непроверенные авто-группы (source='auto', is_confirmed=False) для данного контекста
  - Создаёт новые группы Group(source='auto', is_confirmed=False), обновляет project.group_id
  - Сохраняет SimilarityScore (upsert через pg_insert.on_conflict_do_update)
  - Выставляет is_auto_checked=True всем обработанным проектам
- tests/test_clustering.py: 20 тестов — TestCosineSimMatrix (5), TestApplyRejectedPairs (4), TestCluster (7), TestRunAutoGrouping (4)
- ruff OK, pytest 74 passed
**Следующие шаги:**
- TASK-014: Celery-задача авто-группировки с отслеживанием прогресса через Redis
  - Обернуть run_auto_grouping в @celery.task
  - Создать GroupingRun перед запуском, обновлять статус
  - Redis HASH grouping:progress:{run_id} со stage/current/total
  - API: POST /api/grouping/run, GET /api/grouping/status/{run_id}, GET /api/grouping/history
  - Redis lock (SETNX) для предотвращения двойного запуска
**Заметки для следующей итерации:**
- run_auto_grouping принимает sync Session (psycopg2), не AsyncSession
- grouping_run_id=None по умолчанию — TASK-014 передаст реальный run.id
- Для Celery task: использовать _get_sync_session() из embeddings.py (паттерн одинаковый)
- celery_app.py нужно добавить в include: "app.tasks.clustering"
- GroupingRun.status: pending → running → completed/failed
- Redis ключ для прогресса: "grouping:progress:{run_id}" (HASH), TTL = 1 час после завершения

---

## Итерация 11 — 2026-03-15
**Фича:** TASK-014 — Celery-задача авто-группировки с отслеживанием прогресса через Redis
**Статус:** Завершено
**Что сделано:**
- backend/app/schemas/grouping.py: GroupingRunCreate, GroupingRunRead, GroupingStatusResponse, GroupingRunStartRequest (threshold 0.5–0.95), GroupingRunStartResponse, GroupingHistoryResponse
- backend/app/tasks/grouping.py: Celery task run_grouping_task(run_id, threshold, context) — обновляет GroupingRun.status (pending→running→completed/failed), пишет прогресс в Redis HASH grouping:progress:{run_id} (stage/current/total/status), устанавливает TTL=3600 после завершения, освобождает lock в finally
- backend/app/services/clustering.py: добавлен параметр progress_callback — вызывается на этапах embeddings, similarity, clustering, saving
- backend/app/api/grouping.py: POST /api/grouping/run (Redis lock grouping:lock:{context}, 409 если уже запущено), GET /api/grouping/status/{run_id}, GET /api/grouping/history
- celery_app.py: добавлен "app.tasks.grouping" в include
- main.py: зарегистрирован grouping router
- tests/test_grouping.py: 10 тестов (run OK, run 409 duplicate, status, history, validation)
- ruff OK, pytest 84 passed
**Следующие шаги:**
- TASK-015: Отклонение рекомендаций (rejected_pairs) — depends on TASK-014 (now done)
- TASK-005: Nginx конфигурация (depends on TASK-001, TASK-002 — done)
- TASK-006: Vue frontend (depends on TASK-001 — done)
- TASK-010: Экспорт в Excel (depends on TASK-008 — done)
**Заметки для следующей итерации:**
- Redis lock key: "grouping:lock:{context}" (SETNX, TTL=300 на lock, освобождается в finally)
- Progress HASH key: "grouping:progress:{run_id}" (TTL=3600 после done/failed)
- run_grouping_task использует _get_sync_session() из embeddings.py (psycopg2)
- async Redis для API endpoint через redis.asyncio.from_url(settings.REDIS_URL)
- GroupingRun.started_at устанавливается при переходе в running, completed_at — при завершении

---

## Итерация 12 — 2026-03-15
**Фича:** TASK-006 — Инициализация Vue 3 фронтенда
**Статус:** Завершено
**Что сделано:**
- frontend/src/stores/index.ts, components/index.ts, composables/index.ts, utils/index.ts — созданы пустые placeholder-файлы
- frontend/src/api/client.ts: добавлен import ElMessage; interceptor.response.error теперь вызывает ElMessage.error() с текстом из detail/message/message поля ответа
- frontend/vite.config.ts: убран лишний rewrite (path.replace(/^\/api/, '')) — прокси /api теперь прозрачно форвардит запросы к backend:8000/api/* без потери префикса
- ruff OK, pytest 84 passed
**Следующие шаги:**
- TASK-017: Фронтенд список проектов (depends on TASK-006, TASK-008 — оба done, критический приоритет)
- TASK-010: Экспорт в Excel (depends on TASK-008 — done)
- TASK-015: Отклонение рекомендаций (depends on TASK-014 — done)
**Заметки для следующей итерации:**
- TASK-017 теперь разблокирован — все его зависимости (TASK-006, TASK-008) имеют статус done
- vite.config.ts proxy: /api → http://backend:8000 (без rewrite), так как backend роуты включают /api prefix (например /api/projects)
- ElMessage импортируется из element-plus; уже подключён глобально в main.ts через app.use(ElementPlus)
- stores/, components/, composables/, utils/ — пустые index.ts, наполнить в TASK-017+

---

## Итерация 13 — 2026-03-15
**Фича:** TASK-017 — Фронтенд: список проектов с фильтрами, поиском и пагинацией
**Статус:** Завершено
**Что сделано:**
- frontend/src/api/projects.ts: projectsApi с методами getList (все фильтры + пагинация), getStats, select, deselect; типы ProjectListItem, ProjectListResponse, StatsCounters, ProjectFilters
- frontend/src/api/dictionaries.ts: dictionariesApi.getAll(type, activeOnly) для загрузки направлений/УГТ в фильтры
- frontend/src/stores/projects.ts: Pinia store useProjectsStore — state (items, total, loading, stats, filters), actions (fetchProjects, fetchStats, setFilters, setPage, resetFilters)
- frontend/src/components/ProjectFilters.vue: поисковая строка с debounce 300ms + dropdowns (direction, priority_direction, trl, is_ongoing, has_group, group_source) + кнопка сброса
- frontend/src/views/ProjectsView.vue: el-table с колонками (название, направление, срок, группа-тег с авто/ручной стилизацией, статус), счётчики в шапке, пагинация, sync фильтров с URL query params
- ruff OK, pytest 84 passed
**Следующие шаги:**
- TASK-018: Боковая панель карточки проекта (depends on TASK-017 — now done, critical)
- TASK-019: Чекбоксы + создание ручной группы (depends on TASK-017, TASK-011 — both done)
- TASK-020: Режим 'Группы' — аккордеон (depends on TASK-017, TASK-011 — both done)
- TASK-021: Кнопка 'Найти похожие' + прогресс-бар (depends on TASK-014, TASK-017 — both done)
**Заметки для следующей итерации:**
- ProjectFilters.vue загружает справочники в onMounted через Promise.all; для фильтра group_source: 'auto' и 'manual'
- URL query params: синхронизируются через router.replace({query}) при изменении фильтров, восстанавливаются в onMounted через syncFiltersFromQuery
- Группы: auto → пунктирная рамка тега; manual → сплошная рамка; цвет из палитры GROUP_COLORS по hash от group_id
- ProjectsStore — Pinia singleton, фильтры сохраняются при переходе между вкладками (в т.ч. через URL)
- Для TASK-018 нужно: добавить @row-click handler в таблицу (уже есть console.log), создать ProjectDetailPanel.vue как slide-in drawer
- el-statistic из Element Plus — компонент для счётчиков в шапке

---

## Итерация 14 — 2026-03-15
**Фича:** TASK-018 — Фронтенд: боковая панель карточки проекта (slide-in)
**Статус:** Завершено
**Что сделано:**
- frontend/src/api/projects.ts: добавлены интерфейсы GroupInfo и ProjectRead; добавлен метод getById(id)
- frontend/src/api/groups.ts: создан — интерфейсы GroupProjectItem и GroupRead; методы getById(id) и removeProject(groupId, projectId)
- frontend/src/components/ProjectDetailPanel.vue: el-drawer (direction="rtl", size="480px") с:
  - Загрузка проекта через getById + группы через groupsApi.getById (если есть group)
  - Статус-теги (Срочный/Бессрочный, В отборе/Проверен/Новый)
  - el-descriptions для словарных полей (Направление, Приор. направление, УГТ)
  - Полные текстовые поля без обрезки (white-space: pre-wrap)
  - Секция группы с тегом (пунктирный=авто/сплошной=ручной), тип, статус подтверждения, список других проектов
  - Footer с кнопками: 'Убрать из группы' (если в группе), 'Добавить в отбор' / 'Убрать из отбора'
- frontend/src/views/ProjectsView.vue: onRowClick теперь устанавливает selectedProjectId; ProjectDetailPanel подключён с @close и @updated (рефреш списка + стата)
- ruff OK, pytest 84 passed
**Следующие шаги:**
- TASK-019: Чекбоксы + создание ручной группы (depends on TASK-017, TASK-011 — both done)
- TASK-020: Режим 'Группы' — аккордеон (depends on TASK-017, TASK-011 — both done)
- TASK-021: Кнопка 'Найти похожие' + прогресс-бар (depends on TASK-014, TASK-017 — both done)
**Заметки для следующей итерации:**
- ProjectDetailPanel: props.projectId watch → открыть drawer + loadProject; emit('close') при закрытии → parent сбрасывает selectedProjectId = null
- groupsApi.removeProject возвращает GroupRead (backend так настроен), но мы не используем ответ
- Кнопка 'Добавить в группу' не реализована (требует выбора существующей группы — зависит от TASK-019/020)
- el-drawer из Element Plus обеспечивает встроенную CSS slide-in анимацию

---

## Итерация 0 — Инициализация
**Фича:** Настройка проекта
**Статус:** Ожидает начала
**Что сделано:**
- Создан PRD документ
- Определён технический стек (Python 3.13, uv, aiogram, Supabase)
**Следующие шаги:**
- Инициализировать проект с uv
- Настроить структуру папок
- Добавить базовые зависимости
**Заметки для следующей итерации:**
- Начать с Phase 1: MVP Core
- Приоритет: базовая структура проекта и конфигурация

---
